export enum GameStatus {
    NotStarted = 0,
    //Initializing = 1,
    InProgress = 2,
    Done = 4,

    // TODO: question answered flag? to indicate correct answer but timer is paused
    AnswerCorrect = 8
}
